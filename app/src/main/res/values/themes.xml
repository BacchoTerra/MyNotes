<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Memória" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="colorPrimaryVariant">@color/darkPrimaryColor</item>
        <item name="colorOnPrimary">@color/onPrimary</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/secondaryColor</item>
        <item name="colorOnSecondary">@color/onSecondary</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/darkPrimaryColor</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="CustomNoteTitle" parent="TextAppearance.MaterialComponents.Headline6">
        <item name="android:textColor">@color/noteTitleColor</item>

    </style>

    <style name="CustomNoteDesc" parent="TextAppearance.MaterialComponents.Body2">
        <item name="android:textColor">@color/noteDescColor</item>
        <item name="android:textSize">16sp</item>
    </style>

    <style name="CustomEditText" parent="Widget.AppCompat.EditText" >

        <item name="android:textColorHint">@color/hintTextColor</item>
        <item name="backgroundTint">@color/backgroundColor</item>
        <item name="android:textCursorDrawable">@drawable/cursor_edit_text</item>
        <item name="android:textColor">@color/textOnBackground</item>

    </style>
</resources>